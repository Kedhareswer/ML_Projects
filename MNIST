{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.11.11","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"nvidiaTeslaT4","dataSources":[],"dockerImageVersionId":31011,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":true}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# Notebook & plotting style\nimport matplotlib.pyplot as plt\nimport seaborn as sns\nimport numpy as np\n\n# Seaborn + matplotlib style\nsns.set(style='white', font_scale=1.15)\nplt.rcParams.update({\n    'axes.edgecolor': 'gray',\n    'axes.linewidth': 0.6,\n    'figure.dpi': 100,\n    'figure.facecolor': 'none',\n    'savefig.facecolor': 'none'\n})\n\n# Inline HTML for adaptive styling\nfrom IPython.display import display, HTML\n\ndisplay(HTML('''\n<style>\n    /* Base font and layout */\n    body, div, table, td, th {\n        font-family: \"Inter\", -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif;\n        font-size: 15px;\n        line-height: 1.6;\n    }\n\n    h1, h2, h3 {\n        font-weight: 600;\n        margin-top: 1.5em;\n        margin-bottom: 0.5em;\n    }\n\n    /* Table styling */\n    table {\n        border-collapse: collapse;\n        width: 100%;\n        margin: 1em 0;\n    }\n\n    th, td {\n        text-align: left;\n        padding: 0.6em 0.8em;\n        border: 1px solid #ccc;\n    }\n\n    th {\n        background-color: #f4f4f4;\n    }\n\n    /* Light theme */\n    body {\n        background-color: #ffffff;\n        color: #222222;\n    }\n\n    /* Dark theme support */\n    @media (prefers-color-scheme: dark) {\n        body {\n            background-color: #111111;\n            color: #dddddd;\n        }\n\n        th {\n            background-color: #222222;\n            color: #dddddd;\n        }\n\n        td {\n            border-color: #444;\n        }\n\n        table {\n            border-color: #444;\n        }\n    }\n\n    /* Output area */\n    .jp-OutputArea-output pre, .output pre {\n        font-family: \"Fira Code\", \"Courier New\", monospace;\n        font-size: 14px;\n        background: none;\n        color: inherit;\n    }\n</style>\n'''))","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2025-06-07T20:05:36.175908Z","iopub.execute_input":"2025-06-07T20:05:36.176100Z","iopub.status.idle":"2025-06-07T20:05:38.264677Z","shell.execute_reply.started":"2025-06-07T20:05:36.176083Z","shell.execute_reply":"2025-06-07T20:05:38.263986Z"},"jupyter":{"source_hidden":true}},"outputs":[{"output_type":"display_data","data":{"text/plain":"<IPython.core.display.HTML object>","text/html":"\n<style>\n    /* Base font and layout */\n    body, div, table, td, th {\n        font-family: \"Inter\", -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, sans-serif;\n        font-size: 15px;\n        line-height: 1.6;\n    }\n\n    h1, h2, h3 {\n        font-weight: 600;\n        margin-top: 1.5em;\n        margin-bottom: 0.5em;\n    }\n\n    /* Table styling */\n    table {\n        border-collapse: collapse;\n        width: 100%;\n        margin: 1em 0;\n    }\n\n    th, td {\n        text-align: left;\n        padding: 0.6em 0.8em;\n        border: 1px solid #ccc;\n    }\n\n    th {\n        background-color: #f4f4f4;\n    }\n\n    /* Light theme */\n    body {\n        background-color: #ffffff;\n        color: #222222;\n    }\n\n    /* Dark theme support */\n    @media (prefers-color-scheme: dark) {\n        body {\n            background-color: #111111;\n            color: #dddddd;\n        }\n\n        th {\n            background-color: #222222;\n            color: #dddddd;\n        }\n\n        td {\n            border-color: #444;\n        }\n\n        table {\n            border-color: #444;\n        }\n    }\n\n    /* Output area */\n    .jp-OutputArea-output pre, .output pre {\n        font-family: \"Fira Code\", \"Courier New\", monospace;\n        font-size: 14px;\n        background: none;\n        color: inherit;\n    }\n</style>\n"},"metadata":{}}],"execution_count":1},{"cell_type":"markdown","source":"# Handwritten Digit Recognition with MNIST\n\nIn this notebook, we explore the MNIST dataset — a benchmark dataset for image classification. We'll visualize the data, train a deep learning model using TensorFlow/Keras, and evaluate it using various metrics and visualizations.","metadata":{}},{"cell_type":"code","source":"import warnings\n\n# Ignore all warnings\nwarnings.filterwarnings('ignore')","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-06-07T20:05:38.265727Z","iopub.execute_input":"2025-06-07T20:05:38.267093Z","iopub.status.idle":"2025-06-07T20:05:38.271606Z","shell.execute_reply.started":"2025-06-07T20:05:38.267072Z","shell.execute_reply":"2025-06-07T20:05:38.271064Z"}},"outputs":[],"execution_count":2},{"cell_type":"markdown","source":"### Load and Inspect the MNIST Dataset","metadata":{}},{"cell_type":"code","source":"# Load MNIST dataset\nfrom tensorflow.keras.datasets import mnist\n\n(x_train, y_train), (x_test, y_test) = mnist.load_data()\n\n# Basic info\nprint(f\"Training samples: {x_train.shape[0]} | Shape: {x_train.shape[1:]} (H x W)\")\nprint(f\"Test samples:     {x_test.shape[0]} | Shape: {x_test.shape[1:]} (H x W)\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-06-07T20:05:38.272238Z","iopub.execute_input":"2025-06-07T20:05:38.272477Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Visualize a Grid of Sample Digits","metadata":{}},{"cell_type":"code","source":"# Preview sample digits\nimport matplotlib.pyplot as plt\n\nfig, axes = plt.subplots(2, 5, figsize=(10, 4))\n\nfor i, ax in enumerate(axes.flat):\n    ax.imshow(x_train[i], cmap='gray', interpolation='nearest')\n    ax.set_title(f\"Label: {y_train[i]}\", fontsize=12)\n    ax.axis('off')\n\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Data Preprocessing","metadata":{}},{"cell_type":"code","source":"from tensorflow.keras.utils import to_categorical\n\n# Normalize pixel values\nx_train = x_train.astype(\"float32\") / 255.\nx_test = x_test.astype(\"float32\") / 255.\n\n# One-hot encode labels\ny_train_cat = to_categorical(y_train, 10)\ny_test_cat = to_categorical(y_test, 10)\n\n# Shape check\nprint(f\"x_train shape: {x_train.shape}\")\nprint(f\"y_train_cat shape: {y_train_cat.shape}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Build a Simple Fully Connected Model","metadata":{}},{"cell_type":"code","source":"from tensorflow.keras.models import Sequential\nfrom tensorflow.keras.layers import Dense, Flatten\n\n# Define the model\nmodel = Sequential([\n    Flatten(input_shape=(28, 28)),\n    Dense(128, activation='relu'),\n    Dense(64, activation='relu'),\n    Dense(10, activation='softmax')\n])\n\n# Compile the model\nmodel.compile(\n    optimizer='adam',\n    loss='categorical_crossentropy',\n    metrics=['accuracy']\n)\n\n# Summary of the model\nmodel.summary()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Train the Model","metadata":{}},{"cell_type":"code","source":"history = model.fit(\n    x_train, y_train_cat,\n    epochs=10,\n    batch_size=32,\n    validation_split=0.1,\n    verbose=2\n)","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Plot Training & Validation Curves","metadata":{}},{"cell_type":"code","source":"import matplotlib.pyplot as plt\n\nfig, ax = plt.subplots(1, 2, figsize=(12, 4))\n\n# Accuracy plot\nax[0].plot(history.history['accuracy'], label='Train')\nax[0].plot(history.history['val_accuracy'], label='Validation')\nax[0].set_title('Accuracy')\nax[0].set_xlabel('Epoch')\nax[0].set_ylabel('Accuracy')\nax[0].legend()\nax[0].grid(False)\n\n# Loss plot\nax[1].plot(history.history['loss'], label='Train')\nax[1].plot(history.history['val_loss'], label='Validation')\nax[1].set_title('Loss')\nax[1].set_xlabel('Epoch')\nax[1].set_ylabel('Loss')\nax[1].legend()\nax[1].grid(False)\n\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"###  Evaluate on Test Data and Classification Report","metadata":{}},{"cell_type":"code","source":"from sklearn.metrics import classification_report, accuracy_score\nimport numpy as np\n\n# Predict probabilities and convert to labels\ny_pred_prob = model.predict(x_test)\ny_pred = np.argmax(y_pred_prob, axis=1)\n\n# Overall accuracy\ntest_accuracy = accuracy_score(y_test, y_pred)\nprint(f\"Test Accuracy: {test_accuracy:.4f}\\n\")\n\n# Detailed classification report\nprint(classification_report(y_test, y_pred, digits=4))","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Confusion Matrix Heatmap","metadata":{}},{"cell_type":"code","source":"from sklearn.metrics import confusion_matrix\nimport seaborn as sns\nimport matplotlib.pyplot as plt\n\ncm = confusion_matrix(y_test, y_pred)\n\nplt.figure(figsize=(8, 6))\nsns.heatmap(\n    cm,\n    annot=True,\n    fmt='d',\n    cmap='Greys',\n    cbar=False,\n    square=True,\n    linewidths=0.5,\n    linecolor='gray'\n)\nplt.xlabel('Predicted Label')\nplt.ylabel('True Label')\nplt.title('Confusion Matrix')\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true,"jupyter":{"source_hidden":true}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"### Visualize Misclassified Samples","metadata":{}},{"cell_type":"code","source":"import numpy as np\nimport matplotlib.pyplot as plt\n\nmisclassified_idx = np.where(y_pred != y_test)[0]\n\nplt.figure(figsize=(12, 8))\nfor i, idx in enumerate(misclassified_idx[:20]):\n    plt.subplot(4, 5, i + 1)\n    plt.imshow(x_test[idx], cmap='gray')\n    plt.title(f\"T: {y_test[idx]} | P: {y_pred[idx]}\", fontsize=10)\n    plt.axis('off')\n\nplt.tight_layout()\nplt.show()","metadata":{"trusted":true,"jupyter":{"source_hidden":true}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"# Conclusion\n\n- The fully connected neural network achieved strong accuracy on MNIST, demonstrating effective digit classification.\n- Training and validation curves show good convergence with minimal overfitting.\n- The confusion matrix and misclassified samples help diagnose the few challenging digits.\n- Future improvements could include convolutional layers, data augmentation, and hyperparameter tuning to boost accuracy.\n\nThis notebook provides a clear, minimal, and effective pipeline from data loading to evaluation for MNIST classification.","metadata":{}},{"cell_type":"code","source":"","metadata":{"trusted":true},"outputs":[],"execution_count":null}]}